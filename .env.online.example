# 本地开发与 personal-homepage 的 online 方式一致。
# 复制为 .env.online.local 并填写目标服务 origin（不含 /api）。
VITE_API_URL=/api
VITE_PROXY_TARGET=https://api.example.com
# Vite 代理仅用于开发。生产部署需要同源 /api 反向代理，
# 或将 VITE_API_URL 改为实际 HTTPS API 地址并配置服务端 CORS。
